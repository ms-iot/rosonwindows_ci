resources:
  repositories:
    - repository: templates
      type: github
      name: seanyen/rosonwindows_ci-1
      endpoint: seanyen

jobs:
- template: build.yml@templates  # Template reference
  parameters:
    ros_metapackage: 'ros-melodic-ros_core'
